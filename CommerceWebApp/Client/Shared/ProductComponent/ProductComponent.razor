@inherits CommerceWebApp.Client.Shared.ProductBase
@using CommerceWebApp.Shared
@using Newtonsoft.Json

<button onclick=@(() => SetPage.InvokeAsync(ProductPage.Products))>Back</button>
<pre></pre>
<table>
    <thead>
        <tr>
            <th class="table-data">Name</th>
            <th class="table-data">Id</th>
            <th class="table-data">Price</th>
            <th class="table-data">Stock</th>
            <th class="table-data">Dimensions</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>@(Product!.Name)</td>
            <td>@(Product!.Id)</td>
            <td>@(Product!.Price)</td>
            <td>@(Product!.Stock)</td>
            <td>@(JsonConvert.SerializeObject(Product!.Dimensions))</td>
        </tr>
    </tbody>
</table>